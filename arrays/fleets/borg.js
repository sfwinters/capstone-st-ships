class Starship {
    constructor(type, use) {
        this.type = type,
        this.use = use
    }
}

const sphere = new Starship('Borg sphere', 'Scout ship');
const cube = new Starship('Borg cube', 'Assimilation')